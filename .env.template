# Discord Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Enable only the guilds ChatGPT/MCP may access. Comma-separated IDs.
# Use * only if you intentionally want every guild Lily is in.
DISCORD_ADMIN_GUILD_IDS=

# Optional: enable this only after enabling "Server Members Intent" in the
# Discord Developer Portal. It improves name-based member search.
DISCORD_MEMBERS_INTENT=false

# MCP Configuration
# Leave unset when /mcp/ is reachable only through ChatGPT Secure MCP Tunnel
# or an authenticating private reverse proxy. Generic MCP clients can send:
# Authorization: Bearer <this value>
MCP_BEARER_TOKEN=

# Optional MCP DNS-rebinding allowlists. When MCP_ALLOWED_HOSTS is empty,
# localhost is allowed and the hosted adapter FQDN is derived from DOMAIN_NAME.
# Example: lily-discord-adapter.nstut.cloud,lily-discord-adapter.nstut.cloud:*
MCP_ALLOWED_HOSTS=
# Usually empty for server-to-server MCP clients. Add browser origins only if needed.
MCP_ALLOWED_ORIGINS=

# Service Configuration
PORT=8004

# Consul Configuration
CONSUL_HTTP_ADDR=consul:8500
